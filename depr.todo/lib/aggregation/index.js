/**
 * The 'Aggregation' namespace provides various classes, related to
 * service API aggregation (facade layer creation).
 *
 * @namespace Aggregation
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );

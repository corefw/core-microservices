/**
 * The 'ExecutionContext' namespace provides classes that represent the run-time
 * environment and the client request (collectively the "context"). These
 * classes abstract the dynamics of each context in order to provide the
 * endpoint a uniform interface to its environment and the information related
 * to the request.
 *
 * @namespace ExecutionContext
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );

/**
 * The 'Deploy' namespace provides various classes, related to
 * metadata deployment during CI/CD operations.
 *
 * @namespace Deploy
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );

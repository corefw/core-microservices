/**
 * The 'Deploy.Module' namespace provides various "Deployment Modules", which
 * are individual components that handle the various, configurable, stages of
 * metadata deployment
 *
 * @namespace Deploy.Module
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );

/**
 * The 'Deploy.Target' namespace provides various classes that abstract metadata
 * deployment targets, which modules will use to deploy/create files during
 * metadata deployment operations.
 *
 * @namespace Deploy.Target
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );

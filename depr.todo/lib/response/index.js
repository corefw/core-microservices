/**
 * The 'Response' namespace provides classes that represent API/endpoint
 * responses, which are sent back to API clients after a request has succeeded
 * or failed.
 *
 * @namespace Response
 */

"use strict";

module.exports = require( "requireindex" )( __dirname );
